<mapper namespace="product">
    <select id="selectList">
        select * from user
    </select>

    <select id="selectOne">
        select * from user where id = ? and username = ?
    </select>
</mapper>